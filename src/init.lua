local UI = require(script.createGUI)
local Window = require(script.window)
-- attempt to create a window (For Testing.)
UI.part.CFrame = CFrame.new(0, 10, 0)
Window.new(UI.main, "Test", UDim2.fromScale(0.4, 0.4), true)
